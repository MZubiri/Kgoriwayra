namespace Kgoriwayra.Domain.Enums;

public enum IncludedItemType
{
    Included = 1,        // Qué incluye
    NotIncluded = 2,     // Qué no incluye
    Recommendation = 3,  // Recomendaciones al viajero
    Restriction = 4,     // Restricciones físicas / edad / peso
    SafetyMeasure = 5    // Medidas de seguridad y protocolos
}
